# Angular

```bash
# Update angular cli
sudo npm uninstall -g angular-cli 
sudo npm install -g @angular/cli@latest
```

```bash
# New app
ng new {name} 
# Start dev local server
ng serve 
# Add component
ng g c {path}/{name}
```
